import { Link } from 'gatsby';
import styled from 'styled-components';

/**
 * This does not contain any styles, but makes styling from other components easier.
 */
export const LinkWrapper = styled(Link)``;
